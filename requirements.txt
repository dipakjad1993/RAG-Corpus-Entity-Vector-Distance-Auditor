# Legacy pip entrypoint — single source of truth is pyproject.toml (uv).
# Preferred: `uv sync` (uses uv.lock). This file remains for `pip install -r`
# compat (Docker) and simply re-exports the project dependencies.
-e .
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
