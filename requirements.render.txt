# RAG-EVDA LITE (Render free 512MB) — CPU-only, no heavy ML at boot.
# Installed AFTER `pip install --no-deps -e .` (see Dockerfile.render), so
# this file lists ONLY the light runtime set. The FULL stack (CUDA torch,
# transformers, pandas, reportlab, faiss, fastapi/uvicorn) is deliberately
# absent: torch comes CPU-only from download.pytorch.org, transformers only
# arrives as sentence-transformers' own dependency (disk only, never imported
# at boot — sentiment/reranker are disabled via RAGEVDA_LITE=1).
# NOTE: NO torch (installed CPU-only in Dockerfile.render), NO transformers
# direct, NO pandas, NO reportlab, NO faiss, NO fastapi/uvicorn on free.
flask>=3.0.0
gunicorn
sentence-transformers>=2.3.0
spacy>=3.7.0
numpy<3.0
scikit-learn>=1.3.0
networkx>=3.1.0
huggingface_hub>=0.20.0
duckdb>=0.10.0
trafilatura>=1.6.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.27.0
pyyaml>=6.0.0
ddgs>=4.0.0
pydantic>=2.0.0
